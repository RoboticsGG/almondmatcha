diff --git a/include/rtps/common/types.h b/include/rtps/common/types.h
index 781aba5..33e6127 100644
--- a/include/rtps/common/types.h
+++ b/include/rtps/common/types.h
@@ -188,7 +188,7 @@ struct SequenceNumberSet {
   SequenceNumber_t base = {0, 0};
   // Cannot be static because of packed
   uint32_t numBits = SNS_NUM_BITS;
-  std::array<uint32_t, 1> bitMap{};
+  std::array<uint32_t, 8> bitMap{};  // RTPS spec max: 256 bits
 
   // We only need 1 byte because atm we don't store packets.
   bool isSet(uint32_t bit) const {
diff --git a/include/rtps/messages/MessageTypes.h b/include/rtps/messages/MessageTypes.h
index 97f38af..0282e5d 100644
--- a/include/rtps/messages/MessageTypes.h
+++ b/include/rtps/messages/MessageTypes.h
@@ -236,7 +236,7 @@ struct SubmessageAckNack {
   static uint16_t getRawSize(const SequenceNumberSet &set) {
     uint16_t bitMapSize = 0;
     if (set.numBits != 0) {
-      bitMapSize = 4 * ((set.numBits / 32) + 1);
+      bitMapSize = 4 * ((set.numBits + 31) / 32);
     }
     return getRawSizeWithoutSNSet() + sizeof(SequenceNumber_t) +
            sizeof(uint32_t) + bitMapSize; // SequenceNumberSet
@@ -396,7 +396,7 @@ bool serializeMessage(Buffer &buffer, SubmessageAckNack &msg) {
                 sizeof(uint32_t));
   if (msg.readerSNState.numBits != 0) {
     buffer.append(reinterpret_cast<uint8_t *>(msg.readerSNState.bitMap.data()),
-                  4 * ((msg.readerSNState.numBits / 32) + 1));
+                  4 * ((msg.readerSNState.numBits + 31) / 32));
   }
   buffer.append(reinterpret_cast<uint8_t *>(&msg.count.value),
                 sizeof(msg.count.value));
diff --git a/src/messages/MessageTypes.cpp b/src/messages/MessageTypes.cpp
index 54b5cda..693105d 100644
--- a/src/messages/MessageTypes.cpp
+++ b/src/messages/MessageTypes.cpp
@@ -159,6 +159,11 @@ bool rtps::deserializeMessage(const MessageProcessingInfo &info,
   doCopyAndMoveOn(reinterpret_cast<uint8_t *>(&msg.readerSNState.numBits),
                   currentPos, sizeof(uint32_t));
 
+  // Reject malformed AckNack with numBits exceeding RTPS spec max (256)
+  if (msg.readerSNState.numBits > 256) {
+    return false;
+  }
+
   // Now we can check for full size
   if (remainingSizeAtBeginning <
       SubmessageAckNack::getRawSize(msg.readerSNState)) {
@@ -168,7 +173,7 @@ bool rtps::deserializeMessage(const MessageProcessingInfo &info,
   if (msg.readerSNState.numBits != 0) {
     doCopyAndMoveOn(
         reinterpret_cast<uint8_t *>(msg.readerSNState.bitMap.data()),
-        currentPos, 4 * ((msg.readerSNState.numBits / 32) + 1));
+        currentPos, 4 * ((msg.readerSNState.numBits + 31) / 32));
   }
   doCopyAndMoveOn(reinterpret_cast<uint8_t *>(&msg.count.value), currentPos,
                   sizeof(msg.count.value));
